---
import Default from '@layouts/Default.astro';
import BlogList from '@components/BlogList.astro';
export interface Props {
	pageClass?: string,
  pageTitle: string
}
const { pageClass } = Astro.props
---

<Default layoutClass={`page ${pageClass}`}>
	<BlogList />
</Default>